#This is file contain all the git command i learned during my practice and session

1.Git init -- to initalize an file.

2.Git status -- to check the staus of the repo.

3.Git add -- to add the file from untrack to stagging area.

4.Git rm --cached <file-name> -- it removes stagging files to untrack area.

5.Git commit -- to add the file from staged to track area.

6.Git restore -- to restore delete file.

7.Git config -- It authorize who have commited the file.

8.Git checkout -b -- To create new branch and switch.

9.Git branch -- to check curremt working branch

10.Git switch -- Also works to switch from one branch to another.

11.Git branch -D <branch-name> -- to delete a branch.
